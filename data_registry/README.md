# Data Registry

Store dataset manifests only (paths, version hashes, splits, label hierarchy mappings).
Do not commit raw large datasets.

TODO:
- Add HLCA and additional multi-level annotation dataset manifests.
- Add split protocols for train/val/test and OOD scenarios.
