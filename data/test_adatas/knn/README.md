This folder contains local symlinks to KNN benchmark AnnData files that live
outside the repository.

Current expected files (symlinks):

- `reference_10k.h5ad` → `sampled_adata_knn_10000.h5ad`
- `query_3k.h5ad` → `sampled_adata_knn_3000.h5ad`

These files are not committed; the symlinks are created locally for convenience.

